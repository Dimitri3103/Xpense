import { makeStyles } from "@material-ui/core";

const listExpTypes = makeStyles((theme) => ({
  inputSort: { width: "100%" },
  btnAddTaxes: { width: "100%", height: "2vw" },
}));

export default listExpTypes;
